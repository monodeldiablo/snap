NULL = 

snapdir = ${pkgdatadir}
snap_DATA = \
	snap.svg \
	main.ui \
	$(NULL)

schemadir = ${datadir}/gconf/schemas
schema_DATA = \
	snap.schemas \
	$(NULL)

pcdir = ${libdir}/pkgconfig
pc_DATA = \
	xmpl.pc \
	$(NULL)
